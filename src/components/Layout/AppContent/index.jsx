import { Layout } from "antd";
import styles from "./content.module.scss";

export const AppContent = () => {
  return (
    <Layout.Content className={styles.contentStyle}>Content</Layout.Content>
  );
};
